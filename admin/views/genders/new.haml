%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:genders, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:genders, :new)

.tabs-content
  = form_for :gender, url(:genders, :create), :class => 'form-horizontal' do |f|
    =partial 'genders/form', :locals => { :f => f }
