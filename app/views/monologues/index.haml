.center-column-header

	=render 'shared/search_box_controls', layout: false

.center-column-list
	.jquery-search-replace
		/ Contents of this div replaced by key up triggered jquery ajax call

		- play_title = @play.title if @play
		=partial('monologues/list', locals: {show_play_title: true, play_title: play_title, toggle: "collapse"})

		:javascript
			globalPlayTitle = '#{play_title}';
			globalViewGender = '#{session[:gender]}';
			updateGenderLinks();
			registerGenderClick();
			registerToggleClick();
			
