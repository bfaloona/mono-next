.center-column-header
	%h3= @title

.center-column-list

	- # @monologues is visible to the partial, so we don't need to pass it along

	=partial('monologues/list', locals: {show_play_title: false, play_name: @play&.name})

	:javascript
		gParams["playTitle"] = '#{@play&.title}';
		gParams["gender'] = '#{session[:gender]}';
		updateGenderLinks();
